export type TelegramConfigType = {
  url: string;
  getMe: string;
  sendMessage: string;
  forwardMessage: string;
  sendPhoto: string;
  sendAudio: string;
  sendDocument: string;
  sendSticker: string;
  sendVideo: string;
  sendVoice: string;
  sendLocation: string;
};
